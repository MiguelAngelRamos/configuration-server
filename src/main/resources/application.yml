server:
  port: 8085
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MiguelAngelRamos/microservice-configserver.git
          clone-on-start: true